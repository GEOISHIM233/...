services:
  - type: web
    name: stealer-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server_bot:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
